apply plugin: 'java'

dependencies {
	compile fileTree( dir: "../libs", include: ['JUtils.jar'] )
}

ext {
	copyLibs = true
	deployedLibDir = "Apps/run"
	libDest = "deployed/Tools/libs"
	jarPathPrefix = "../../libs/"
	
	mainClass = "mc.MatrixTest"
	iconFile = ''
}

apply from: '../gradle/utilapp.gradle'